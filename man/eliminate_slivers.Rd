% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eliminate_slivers.R
\name{eliminate_slivers}
\alias{eliminate_slivers}
\alias{desliver}
\title{Eliminate sliver polygons by merging them into a neighbour}
\usage{
eliminate_slivers(
  x,
  threshold,
  keep = NULL,
  explode = TRUE,
  boundary = TRUE,
  overlap = FALSE,
  distance = TRUE,
  minover = 0.05,
  maxdist = 1,
  append = TRUE
)

desliver(
  x,
  threshold,
  keep = NULL,
  explode = TRUE,
  boundary = TRUE,
  overlap = FALSE,
  distance = TRUE,
  minover = 0.05,
  maxdist = 1,
  append = TRUE
)
}
\arguments{
\item{x}{an \code{sf} or \code{SpatVector} polygons object. A projected CRS is
expected, so areas and border lengths are metric; warns otherwise.}

\item{threshold}{sliver-area cutoff. Either a \pkg{units} object (e.g.
\code{units::set_units(1, "ha")}) or a bare numeric in square metres. Polygons
with area \verb{<= threshold} are candidate slivers.}

\item{keep}{optional <\code{\link[rlang:args_data_masking]{data-masking}}> predicate
evaluated against \code{x}'s attribute table; features for which it is \code{TRUE}
are never treated as slivers, even when below \code{threshold}. Column names
are used directly. Defaults to \code{NULL} (slivers by area alone). See
examples.}

\item{explode}{if \code{TRUE} (the default), multipart polygons are split to
single part before sliver detection, so \code{threshold} applies to each
individual ring. When \code{TRUE}, prefer a column-based \code{keep} predicate over
an external vector, since exploding changes the number/order of features.}

\item{boundary, overlap, distance, minover, maxdist, append}{passed through to
\code{\link[terra:combineGeoms]{terra::combineGeoms()}}. The defaults implement the \emph{LENGTH} rule:
\code{boundary = TRUE} assigns each sliver to the keeper it shares the longest
border with; \code{overlap = FALSE} disables overlap-area precedence;
\code{distance} (within \code{maxdist} map units) and \code{append = TRUE} only affect
slivers that share no border with any keeper (the latter keeps them rather
than dropping them). \code{combineGeoms()} always uses \code{dissolve}/\code{erase} TRUE.}
}
\value{
an object of the same class as \code{x}, with slivers eliminated. If \code{x}
has no slivers it is returned unchanged; if \strong{every} feature is a sliver a
warning is issued and \code{x} is returned unchanged (so the call is safe in a
pipe).
}
\description{
Emulates the ArcGIS \code{Eliminate} tool (the \emph{LENGTH} option): small "sliver"
polygons are removed by merging each into the neighbouring polygon with
which it shares the longest border. This is the geometrically-sensible way
to clean up the slivers left behind when two polygon layers that do not
align (e.g. Landscape Units vs. Natural Resource Districts) are intersected,
and is preferable to area-thresholding crumbs away (see \code{\link[=intersect_clean]{intersect_clean()}}).
}
\details{
The merge is delegated to \code{\link[terra:combineGeoms]{terra::combineGeoms()}}; the defaults here
(\code{boundary = TRUE}, \code{overlap = FALSE}) give pure longest-shared-border
assignment. \code{distance}/\code{maxdist} only come into play for slivers that share
no edge with any keeper (e.g. they touch at a point, or sit in a small gap).

\code{x} is returned as the same class it was passed in as ("return the type
that was passed"): an \code{sf} object in yields an \code{sf} object out, a
\code{SpatVector} in yields a \code{SpatVector} out. Internally the work is done with
\pkg{terra}.

Slivers are merged into keepers, so the \strong{attributes of the keeper are
retained} and a sliver's own attributes are discarded (as in ArcGIS
\code{Eliminate}). Use \code{keep} to protect features that fall below \code{threshold}
but should never be dissolved away.
}
\examples{
library(sf)

sq <- function(xmin, xmax, ymin, ymax, id) {
  p <- st_polygon(list(rbind(
    c(xmin, ymin), c(xmax, ymin), c(xmax, ymax), c(xmin, ymax), c(xmin, ymin)
  )))
  st_sf(id = id, protected = id == "B", geometry = st_sfc(p, crs = 3005))
}
polys <- rbind(
  sq(0, 5, 0, 5, "A"), # 25; shares a length-5 border with the sliver
  sq(0, 1, 6, 16, "B"), # 10; shares a length-1 border with the sliver
  sq(0, 5, 5, 6, "S") #  5; the sliver
)

## merge polygons < 6 (map units^2) into their longest-border neighbour:
## "S" is absorbed by "A" (border length 5 > 1); "B" is untouched.
eliminate_slivers(polys, threshold = 6)

## a units threshold works too (areas here are tiny, shown for syntax):
eliminate_slivers(polys, threshold = units::set_units(6, "m^2"))

## protect features via a data-masking predicate on the attribute table
## ("S" is the only sliver here, but this is how you keep a small feature):
eliminate_slivers(polys, threshold = 6, keep = protected)
eliminate_slivers(polys, threshold = 6, keep = id == "S")
}
\seealso{
\code{\link[terra:combineGeoms]{terra::combineGeoms()}}, \code{\link[=intersect_clean]{intersect_clean()}}, \code{\link[=st_union_analysis]{st_union_analysis()}}
}
