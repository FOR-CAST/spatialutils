% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sym_difference.R
\name{sym_difference}
\alias{sym_difference}
\alias{sym_difference.sf}
\alias{sym_difference.SpatVector}
\alias{sym_difference_tiled}
\title{Symmetric difference of two polygon layers}
\usage{
sym_difference(x, y)

\method{sym_difference}{sf}(x, y)

\method{sym_difference}{SpatVector}(x, y)

sym_difference_tiled(x, y, tiles)
}
\arguments{
\item{x, y}{\code{SpatVector} polygon layers. Either may be empty, in which case the other is returned
whole with its \code{source} tag.}

\item{tiles}{\code{SpatVector} or \code{sf} of tile polygons covering both layers.}
}
\value{
\code{SpatVector} polygons with a single character column, \code{source}, valued \code{"x"} or \code{"y"}.
}
\description{
The parts of \code{x} and \code{y} that do not overlap: \verb{(x \\ y)} together with \verb{(y \\ x)}. Built from two
\code{\link[=erase_polygons]{erase_polygons()}} calls, so it inherits the same guards -- the empty-layer case, the
non-polygonal residue an overlay leaves behind, and row alignment.
}
\details{
\code{sym_difference_tiled()} does the same tile by tile, for the same reason as
\code{\link[=erase_polygons_tiled]{erase_polygons_tiled()}}: difference cost is superlinear in the complexity of the layer being
subtracted. Geometries crossing a seam come back SPLIT; dissolve after this returns, never per
tile. Measure before adopting -- tiling adds a crop per tile and only pays when the layers are
complex enough for that to be worth it.
}
\section{Why the result carries no attributes}{

The two halves come from different layers, which in general have different columns. \code{rbind()} on
\code{SpatVector}s matches columns by NAME and silently NA-fills any a side is missing, so a naive
combine reports "this attribute is NA here" where the truth is "this attribute does not exist on
this side" -- indistinguishable from a real result, and wrong in the quiet direction. Rather than
guess a reconciliation, this returns geometry plus a \code{source} column saying which layer each part
came from. Join attributes back yourself if you need them.
}

\seealso{
\code{\link[=erase_polygons]{erase_polygons()}}, \code{\link[=keep_polygons]{keep_polygons()}}

Other spatial helpers:
\code{\link[=calc_centroid]{calc_centroid()}},
\code{\link[=create_bbox]{create_bbox()}},
\code{\link[=extract_coords]{extract_coords()}},
\code{\link[=prep_agg_lonlat_raster]{prep_agg_lonlat_raster()}},
\code{\link[=repair_geoms]{repair_geoms()}},
\code{\link[=save_gpkg]{save_gpkg()}},
\code{\link[=spatial_join_intersects]{spatial_join_intersects()}}
}
\concept{spatial helpers}
