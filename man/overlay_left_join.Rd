% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/overlay_left_join.R
\name{overlay_left_join}
\alias{overlay_left_join}
\title{Tag one polygon layer with another's attributes, as an overlay}
\usage{
overlay_left_join(x, y)
}
\arguments{
\item{x}{\code{SpatVector} polygons; the left-hand layer, whose footprint is kept.}

\item{y}{\code{SpatVector} polygons whose attributes are carried onto \code{x}. May be
empty or miss \code{x} entirely, in which case \code{y}'s columns come back all \code{NA}.}
}
\value{
\code{SpatVector} polygons covering exactly \code{x}, carrying the columns of
\code{x} followed by those of \code{y} that \code{x} did not already have.
}
\description{
A left join done as an overlay: every part of \code{x} keeps its own attributes
and gains \code{y}'s wherever the two overlap, \code{NA} everywhere else. \code{x}'s
footprint is preserved exactly -- no area invented, none lost, and none
counted twice -- with geometries split at the overlay boundaries so that each
location carries exactly one set of values.
}
\section{Why not a spatial join}{

\code{\link[sf:st_join]{sf::st_join()}} is not an overlay. It keeps whole geometries from \code{x} and
emits one copy per feature of \code{y} they touch, so a polygon spanning two of
\code{y}'s features comes back twice at full extent, each copy carrying a
different set of \code{y}'s values. Measured on one study area, joining a
disturbance layer to a forest inventory that way inflated 4.02 M polygons /
4.09 Mha into 7.78 M polygons / 39.5 Mha.
}

\section{Why not a union}{

\code{terra::union()} has the documented semantics of the ArcGIS \code{Union} tool,
which is what makes it the obvious choice, and on real geometry it returned
output polygons overlapping one another \emph{and} a dissolved footprint smaller
than its input -- double-counting some land while dropping other land
outright, in opposite directions, which is what makes the totals look
plausibly rather than obviously wrong. Measured on a 2,933-polygon window it
gave 74,959.504 ha against a base of 74,738.411 ha with a dissolved footprint
of 74,656.115 ha: 303 ha double-counted, 82 ha lost, and 233 s against 8 s
here.

Reported as \url{https://github.com/rspatial/terra/issues/2175} and fixed
upstream. Intersection and difference partition \code{x} exactly, so the result
here is a partition by construction rather than by trust.
}

\seealso{
\code{\link[=erase_polygons]{erase_polygons()}}, \code{\link[=intersect_relate]{intersect_relate()}}, \code{\link[=intersect_clean]{intersect_clean()}}
}
