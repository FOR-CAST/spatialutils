% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landbase.R
\name{read_vector_aoi}
\alias{read_vector_aoi}
\title{Read a vector layer, filtered to an area of interest}
\usage{
read_vector_aoi(src, aoi, fields = NULL, layer = NULL, relation = "intersects")
}
\arguments{
\item{src}{A \code{SpatVector}, or a file path / data source readable by
\code{\link[terra:vect]{terra::vect()}} (e.g. a shapefile, GeoPackage, or File Geodatabase).}

\item{aoi}{A \code{SpatVector} (or a source readable by \code{\link[terra:vect]{terra::vect()}}) defining
the area of interest. May be in any CRS.}

\item{fields}{Optional character vector of attribute columns to keep. Columns
not present in the source are ignored; \code{NULL} (the default) keeps all.}

\item{layer}{Optional layer name, for sources (e.g. a File Geodatabase) with
more than one layer.}

\item{relation}{Character. The spatial predicate passed to
\code{\link[terra:relate]{terra::relate()}} used to refine the GDAL filter (default
\code{"intersects"}).}
}
\value{
A \code{SpatVector} in the source CRS, containing only the features of
\code{src} related to \code{aoi} and only the requested \code{fields}. Callers should test
\code{nrow() == 0L} for the no-overlap case.
}
\description{
Read a (potentially very large) vector source while pushing the spatial
filter down to the GDAL/OGR read, so that only geometries overlapping
\code{aoi} are materialised in R. This is the memory-efficient way to work with
enormous polygon datasets (e.g. provincial landbase coverages): the bulk of
the features never enter R.
}
\details{
The read proceeds in three steps:
\enumerate{
\item the source CRS is obtained without reading any geometries (via a
\code{\link[terra:vect]{terra::vect()}} \emph{proxy}), and \code{aoi} is projected into it;
\item the layer is read via \code{\link[terra:query]{terra::query()}} on the proxy, pushing \strong{both} the
column selection (\code{vars = fields}) and a spatial \code{filter} (the projected
\code{aoi}) down to the GDAL/OGR read -- so wide attribute tables (a landbase
coverage can have >100 columns) are never materialised in R. GDAL
guarantees all extent-overlapping features are returned but \emph{may} return
extras;
\item the result is refined with \code{\link[terra:relate]{terra::relate()}} so exactly the features
satisfying \code{relation} are kept.
}

If the driver cannot honour the pushed-down read it falls back to a spatial
\code{filter}-only read with the columns subset in R; the \code{relate()} refinement
(and a final column subset) run regardless, so the result is correct even
when a driver silently ignores the pushed-down filter.
}
\examples{
aoi <- terra::vect("POLYGON ((0 0, 5 0, 5 5, 0 5, 0 0))")
terra::crs(aoi) <- "EPSG:3857"
near <- terra::vect("POLYGON ((1 1, 2 1, 2 2, 1 2, 1 1))")
far <- terra::vect("POLYGON ((20 20, 21 20, 21 21, 20 21, 20 20))")
src <- rbind(near, far)
terra::crs(src) <- "EPSG:3857"
read_vector_aoi(src, aoi)
}
