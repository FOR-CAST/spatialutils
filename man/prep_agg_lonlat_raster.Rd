% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_helpers.R
\name{prep_agg_lonlat_raster}
\alias{prep_agg_lonlat_raster}
\title{Prepare an aggregated lon/lat raster for climate-grid retrieval}
\usage{
prep_agg_lonlat_raster(ext, rtm, agg_to = 1000)
}
\arguments{
\item{ext}{A \code{SpatExtent} (or object coercible to one by \code{\link[terra:extend]{terra::extend()}})
giving the target extent.}

\item{rtm}{A \code{SpatRaster} template ("raster to match") supplying the source
resolution and CRS.}

\item{agg_to}{Numeric. Target resolution (in the units of \code{rtm}) used to
compute the aggregation factor relative to \code{terra::res(rtm)}. Defaults to
\code{1000}.}
}
\value{
A \code{SpatRaster} extended, reprojected to EPSG:4326, and aggregated.
}
\description{
Extends a template raster to a target extent, reprojects it to a geographic
(lon/lat, EPSG:4326) coordinate reference system, and aggregates it to an
approximate target resolution. This is used to prepare a coarse point grid
for climate-data retrieval (e.g. BioSIM, ERA5).
}
\examples{
r <- terra::rast(
  xmin = 0, xmax = 100, ymin = 0, ymax = 100,
  resolution = 10, crs = "epsg:3005"
)
terra::values(r) <- seq_len(terra::ncell(r))
prep_agg_lonlat_raster(terra::ext(r), r, agg_to = 50)
}
\seealso{
Other spatial helpers:
\code{\link[=calc_centroid]{calc_centroid()}},
\code{\link[=create_bbox]{create_bbox()}},
\code{\link[=extract_coords]{extract_coords()}},
\code{\link[=save_gpkg]{save_gpkg()}},
\code{\link[=spatial_join_intersects]{spatial_join_intersects()}}
}
\concept{spatial helpers}
