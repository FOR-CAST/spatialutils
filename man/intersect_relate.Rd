% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landbase.R
\name{intersect_relate}
\alias{intersect_relate}
\title{Intersect two vectors, skipping non-intersecting features}
\usage{
intersect_relate(x, y)
}
\arguments{
\item{x, y}{\code{SpatVector}s (or sources readable by \code{\link[terra:vect]{terra::vect()}}).}
}
\value{
A \code{SpatVector} of the intersection of the intersecting subsets of
\code{x} and \code{y}; an empty \code{SpatVector} when they are disjoint.
}
\description{
A \code{terra}-native intersection that first drops the features of each input
that cannot possibly intersect the other (via \code{\link[terra:relate]{terra::relate()}}), so the
underlying GEOS overlay only processes candidate geometries. Equivalent in
result to \code{\link[terra:intersect]{terra::intersect()}}, but avoids feeding it large, mostly-disjoint
inputs.
}
\examples{
x <- terra::vect("POLYGON ((0 0, 3 0, 3 3, 0 3, 0 0))")
y <- terra::vect("POLYGON ((2 2, 5 2, 5 5, 2 5, 2 2))")
intersect_relate(x, y)
}
