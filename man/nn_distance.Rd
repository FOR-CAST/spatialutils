% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_distance.R
\name{nn_distance}
\alias{nn_distance}
\title{Distance from each feature to its nearest neighbour}
\usage{
nn_distance(x, y = NULL, radii = c(0, 250, 1000, 4000, 16000, 64000))
}
\arguments{
\item{x}{A \code{SpatVector}, or an object coercible to one via \code{\link[terra:vect]{terra::vect()}}.}

\item{y}{Optional \code{SpatVector} to measure distances to. When \code{NULL} (the
default), distances are measured within \code{x}, excluding each feature from
its own candidate set.}

\item{radii}{Numeric vector of search radii in map units, smallest first. The
default steps from touching (\code{0}) up by factors of four. A final radius
spanning the whole extent is always appended, so the search cannot run out
of rounds while a neighbour still exists.}
}
\value{
A numeric vector of distances, one per feature of \code{x}, in map units.
\code{NA} for a feature with no neighbour at all (only possible when \code{x} has a
single feature and \code{y} is \code{NULL}, or \code{y} is empty).
}
\description{
Exact distance from every feature of \code{x} to the nearest feature of \code{y} --
or, when \code{y} is \code{NULL}, to the nearest \emph{other} feature of \code{x}.
}
\details{
The obvious implementation, calling \code{\link[sf:st_nearest_feature]{sf::st_nearest_feature()}} once per
feature against everything else, rebuilds the GEOS index on every call. On a
70,000-polygon layer that costs seconds \emph{per polygon}. This instead runs one
indexed query per round over all outstanding features at once, and computes
exact geometry-to-geometry distances only for the candidates that query
returns.

Rounds proceed from the smallest radius up:
\itemize{
\item radius \code{0} is a plain intersects test, so touching or overlapping features
are resolved at distance 0 with no geometry work at all;
\item each later round buffers only the features still unresolved and looks for
candidates within that radius.
}

Most features resolve in the first round or two, so the expensive exact
distance step sees a handful of nearby candidates rather than everything
within one generous fixed radius.

A buffered polygon approximates a circle with straight segments and therefore
sits slightly \emph{inside} the true circle. A distance found near the rim of the
search radius might have a nearer neighbour in the sliver the approximation
cut off, so only distances comfortably inside the radius are accepted; the
rest fall through to the next, larger round.
}
\examples{
squares <- terra::vect(c(
  "POLYGON ((0 0, 100 0, 100 100, 0 100, 0 0))",
  "POLYGON ((300 0, 400 0, 400 100, 300 100, 300 0))",
  "POLYGON ((0 300, 100 300, 100 400, 0 400, 0 300))"
))
terra::crs(squares) <- "EPSG:3005"
nn_distance(squares)
}
\seealso{
Other SpatVector helpers:
\code{\link[=dissolve_by]{dissolve_by()}},
\code{\link[=drop_values]{drop_values()}},
\code{\link[=expanse_planar]{expanse_planar()}}
}
\concept{SpatVector helpers}
