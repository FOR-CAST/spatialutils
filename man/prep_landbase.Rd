% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/landbase.R
\name{prep_landbase}
\alias{prep_landbase}
\title{Prepare a landbase-status polygon layer}
\usage{
prep_landbase(
  src,
  aoi,
  status_col,
  layer = NULL,
  crs = NULL,
  status_out = "lbstatus",
  dissolve = c("raster", "vector", "none"),
  dissolve_res = 30,
  mask = TRUE
)
}
\arguments{
\item{src}{A \code{SpatVector}, or a file path / data source readable by
\code{\link[terra:vect]{terra::vect()}} (e.g. a shapefile, GeoPackage, or File Geodatabase).}

\item{aoi}{A \code{SpatVector} (or a source readable by \code{\link[terra:vect]{terra::vect()}}) defining
the area of interest. May be in any CRS.}

\item{status_col}{Character. Name of the source attribute holding the
landbase status (e.g. \code{"f_active"}, \code{"LBC_LBStatus"}, \code{"F_CONDITIO"},
\code{"LBC_Landbase"}).}

\item{layer}{Optional layer name, for sources (e.g. a File Geodatabase) with
more than one layer.}

\item{crs}{Target coordinate reference system, in any form accepted by
\code{\link[terra:project]{terra::project()}}. Defaults to the CRS of \code{aoi}.}

\item{status_out}{Character. Name to give the standardised status column in
the output (default \code{"lbstatus"}).}

\item{dissolve}{How to dissolve features by status: \code{"raster"} (the default,
fast, grid-snapped), \code{"vector"} (exact but slow), or \code{"none"} (keep
individual features).}

\item{dissolve_res}{Numeric. Grid resolution (in the units of the source CRS)
for \code{dissolve = "raster"} (default \code{30}).}

\item{mask}{Logical. If \code{TRUE} (the default), clip the result precisely to
the \code{aoi} polygon; if \code{FALSE}, keep whole (intersecting) features.}
}
\value{
A \code{SpatVector} in \code{crs}, with a single \code{status_out} column, dissolved
by status (unless \code{dissolve = "none"}) and clipped to \code{aoi} (when
\code{mask = TRUE}). Empty (\code{nrow == 0}) when no landbase features fall in \code{aoi}.
}
\description{
Port of the per-FMA "active/passive" (a.k.a. contributing/non-contributing)
landbase-status processing formerly repeated across the \code{LandWeb_preamble}
study-area helpers. Reads a landbase coverage, keeps only the polygons within
\code{aoi} and only the status column, repairs and dissolves them by status, and
clips the result to \code{aoi}.
}
\details{
The read is spatially and column-filtered up front (see \code{\link[=read_vector_aoi]{read_vector_aoi()}}),
so an enormous source coverage is reduced to the AOI before any geometry
repair or dissolve, and only the handful of dissolved polygons are reprojected
and clipped.

Two dissolve methods are offered because an exact vector union of a large
landbase (hundreds of thousands of overlay polygons) is very slow -- minutes
to hours for the whole thing:
\itemize{
\item \code{"raster"} (the default) rasterises the features by status at \code{dissolve_res}
and polygonises the result -- orders of magnitude faster, with boundaries
snapped to the \code{dissolve_res} grid. For reporting polygons (which are
rasterised to the simulation grid downstream anyway) this is immaterial as
long as \code{dissolve_res} is finer than the analysis grid.
\item \code{"vector"} uses an exact \code{\link[terra:aggregate]{terra::aggregate()}} union (v2-equivalent
boundaries), at a large time cost.
}
}
\examples{
aoi <- terra::vect("POLYGON ((0 0, 4 0, 4 4, 0 4, 0 0))")
terra::crs(aoi) <- "EPSG:3857"
a1 <- terra::vect("POLYGON ((0 0, 2 0, 2 2, 0 2, 0 0))")
a2 <- terra::vect("POLYGON ((2 0, 4 0, 4 2, 2 2, 2 0))")
p1 <- terra::vect("POLYGON ((0 2, 4 2, 4 4, 0 4, 0 2))")
src <- rbind(a1, a2, p1)
src$f_active <- c("Active", "Active", "Passive")
terra::crs(src) <- "EPSG:3857"
prep_landbase(src, aoi, status_col = "f_active")
}
\seealso{
\code{\link[=read_vector_aoi]{read_vector_aoi()}}, \code{\link[=intersect_relate]{intersect_relate()}}
}
