% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prep_vector.R
\name{prep_vector}
\alias{prep_vector}
\title{Crop, mask, and reproject a vector to a study area}
\usage{
prep_vector(
  x,
  studyArea,
  crs = terra::crs(studyArea),
  mask = TRUE,
  makeValid = TRUE,
  buffer = 0.1
)
}
\arguments{
\item{x}{A \code{SpatVector}, or a file path or data source readable by
\code{\link[terra:vect]{terra::vect()}}.}

\item{studyArea}{A \code{SpatVector} (or a source readable by \code{\link[terra:vect]{terra::vect()}})
defining the area of interest, to which \code{x} is clipped. May be in any CRS.}

\item{crs}{Target coordinate reference system, in any form accepted by
\code{\link[terra:project]{terra::project()}}. Defaults to the CRS of \code{studyArea}.}

\item{mask}{Logical. If \code{TRUE} (the default), clip \code{x} to the \code{studyArea}
polygon geometry; if \code{FALSE}, crop to its (buffered) extent only.}

\item{makeValid}{Logical. If \code{TRUE} (the default), repair geometry with
\code{\link[terra:makeValid]{terra::makeValid()}} before and after each geometry operation.}

\item{buffer}{Numeric. Fraction of the larger study-area extent dimension by
which to expand the source-CRS pre-crop extent (default \code{0.1}); \code{0}
disables the buffer. The pre-crop extent is always clamped to \code{x}'s own
extent.}
}
\value{
A \code{SpatVector} reprojected to \code{crs} and clipped to \code{studyArea},
retaining only the attributes of \code{x}.
}
\description{
A lean, \code{terra}-based replacement for the crop/mask/reproject that
\code{reproducible::prepInputs()} performed internally (via \code{postProcessTo()}),
without the caching, options, and download machinery. Intended to run after a
layer has been fetched and read, e.g. with
\code{workflowtools::drive_download_once()} +
\code{workflowtools::archive_extract_once()} + \code{\link[terra:vect]{terra::vect()}}.
}
\details{
The processing mirrors what \code{prepInputs()} does, in this order:
\enumerate{
\item repair \code{x} (and \code{studyArea}) topology with \code{\link[terra:makeValid]{terra::makeValid()}};
\item \strong{pre-crop in the source CRS} -- project the (small) \code{studyArea} into
\code{x}'s CRS, take a buffered extent, and crop \code{x} to it, so large or complex
layers are reduced \emph{before} the costly reprojection (rather than
reprojecting the whole source);
\item reproject the reduced layer to \code{crs};
\item clip precisely to the \code{studyArea} polygon (when \code{mask = TRUE}).
}

Geometry validity is repaired after \strong{every} crop/mask/reproject step, since
each of those operations can introduce invalid geometries.
}
\examples{
studyArea <- terra::vect("POLYGON ((0 0, 10 0, 10 10, 0 10, 0 0))")
terra::crs(studyArea) <- "EPSG:3857"
x <- terra::vect("POLYGON ((8 4, 12 4, 12 6, 8 6, 8 4))")
terra::crs(x) <- "EPSG:3857"
prep_vector(x, studyArea)
}
