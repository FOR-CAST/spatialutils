% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatvector.R
\name{dissolve_by}
\alias{dissolve_by}
\title{Dissolve a vector layer on one or more attributes}
\usage{
dissolve_by(x, by, explode = TRUE)
}
\arguments{
\item{x}{A \code{SpatVector}, or an object coercible to one via \code{\link[terra:vect]{terra::vect()}}.}

\item{by}{Character vector of attribute names to dissolve on.}

\item{explode}{Logical. If \code{TRUE} (the default), split multipart geometries
back to single-part with \code{\link[terra:disagg]{terra::disagg()}}.}
}
\value{
A \code{SpatVector} with one (or, when \code{explode = TRUE}, one or more)
feature per combination of \code{by} values present in \code{x}, carrying only the
\code{by} columns.
}
\description{
Merges features that share the same values of \code{by} into a single geometry,
then (by default) splits multipart results back to single-part features.
}
\details{
This exists because \code{\link[terra:aggregate]{terra::aggregate()}} cannot dissolve on \strong{several}
columns when any of them contains \code{NA}: it returns a \code{SpatVector} whose
attribute table has fewer rows than it has geometries, which then errors on
the next access (\verb{nrow dataframe does not match nrow geometry}). An
unclassified category is usually still a category -- an unclassified seral
stage, an unmapped landbase status -- so dropping or corrupting it is rarely
what a dissolve is meant to do. Here \code{NA} values are encoded to a sentinel
before aggregating and restored afterwards, so every \code{NA} group survives as a
group of its own.

The \code{agg_n} bookkeeping column \code{\link[terra:aggregate]{terra::aggregate()}} adds is removed.
}
\examples{
v <- terra::vect(c(
  "POLYGON ((0 0, 1 0, 1 1, 0 1, 0 0))",
  "POLYGON ((1 0, 2 0, 2 1, 1 1, 1 0))",
  "POLYGON ((3 0, 4 0, 4 1, 3 1, 3 0))"
))
v$class <- c("a", "a", NA)

## the NA feature survives as its own group
as.data.frame(dissolve_by(v, "class"))
}
\seealso{
Other SpatVector helpers:
\code{\link[=drop_values]{drop_values()}},
\code{\link[=expanse_planar]{expanse_planar()}},
\code{\link[=nn_distance]{nn_distance()}}
}
\concept{SpatVector helpers}
