% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatvector.R
\name{expanse_planar}
\alias{expanse_planar}
\title{Planar area of a vector layer}
\usage{
expanse_planar(x, unit = "m")
}
\arguments{
\item{x}{A \code{SpatVector}, or an object coercible to one via \code{\link[terra:vect]{terra::vect()}}.}

\item{unit}{Character. Output unit, passed to \code{\link[terra:expanse]{terra::expanse()}}: one of
\code{"m"} (the default), \code{"km"}, \code{"ha"}.}
}
\value{
A numeric vector of areas in \code{unit}, one per feature.
}
\description{
Area measured in the layer's own projection, which is what \code{\link[sf:st_area]{sf::st_area()}}
returns and what ArcGIS reports as \code{Shape_Area}.
}
\details{
\code{\link[terra:expanse]{terra::expanse()}} defaults to \code{transform = TRUE}, which reprojects to
lon/lat and returns \emph{geodesic} area instead. The two agree in an equal-area
projection (e.g. BC Albers) and disagree everywhere else: in Canada Atlas
Lambert at BC latitudes geodesic area runs about 2.8\% high, which is enough
to move features across an area threshold and to shift every reported total.
Prefer this wherever an area has to line up with \code{sf}, with ArcGIS, or with a
fixed cutoff.

For a layer in a geographic (lon/lat) CRS, planar area is meaningless and
\code{terra} returns geodesic area regardless; this warns in that case.
}
\examples{
v <- terra::vect("POLYGON ((0 0, 100 0, 100 100, 0 100, 0 0))")
terra::crs(v) <- "EPSG:3005"
expanse_planar(v, "m")
}
\seealso{
\code{\link[terra:expanse]{terra::expanse()}}

Other SpatVector helpers:
\code{\link[=dissolve_by]{dissolve_by()}},
\code{\link[=drop_values]{drop_values()}},
\code{\link[=nn_distance]{nn_distance()}}
}
\concept{SpatVector helpers}
