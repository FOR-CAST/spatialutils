% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatvector.R
\name{drop_values}
\alias{drop_values}
\title{Drop all attributes from a vector layer, keeping its geometries}
\usage{
drop_values(x)
}
\arguments{
\item{x}{A \code{SpatVector}, or an object coercible to one via \code{\link[terra:vect]{terra::vect()}}.}
}
\value{
A \code{SpatVector} with the same geometries and no attributes.
}
\description{
\code{terra} has no equivalent of \code{x[, character(0)]}: subsetting a \code{SpatVector}
to zero columns is an error, and \code{x$col <- NULL} has to be repeated for every
column. This assigns an empty attribute table instead.
}
\details{
Useful when a layer is only wanted for its geometry -- as the \code{y} of an
\code{\link[terra:erase]{terra::erase()}}, for instance, where the attributes are irrelevant and
carrying them only bloats the intermediate.
}
\examples{
v <- terra::vect("POLYGON ((0 0, 1 0, 1 1, 0 1, 0 0))")
v$keep_me <- "no"
names(drop_values(v))
}
\seealso{
Other SpatVector helpers:
\code{\link[=dissolve_by]{dissolve_by()}},
\code{\link[=expanse_planar]{expanse_planar()}},
\code{\link[=nn_distance]{nn_distance()}}
}
\concept{SpatVector helpers}
