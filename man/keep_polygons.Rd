% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/keep_polygons.R
\name{keep_polygons}
\alias{keep_polygons}
\title{Keep only the polygonal part of an overlay result}
\usage{
keep_polygons(x)
}
\arguments{
\item{x}{\code{sf} object}
}
\value{
\code{sf} object holding only non-empty \code{POLYGON} / \code{MULTIPOLYGON}
features. May have \strong{more} rows than \code{x}, when a collection held several
polygons, and fewer when a feature had no polygonal part at all.
}
\description{
An overlay can leave a line or a point behind: inside a \code{GEOMETRYCOLLECTION},
or -- where everything left of a polygon is a shared edge -- as a bare
\code{LINESTRING} sitting directly in the geometry column. Only the polygons carry
area, and the residue causes trouble downstream out of all proportion to the
area it represents, which is none.
}
\section{Why testing for \code{GEOMETRYCOLLECTION} is not enough}{

Two polygons that share only an edge intersect in a bare \code{LINESTRING}, with
no collection anywhere in the result, so a guard written as
\verb{if (any(st_geometry_type(x) == "GEOMETRYCOLLECTION"))} never fires.

The way that fails is worth spelling out. \code{\link[terra:vect]{terra::vect()}} \strong{drops} a
non-polygon geometry while keeping its attribute row, so the \code{SpatVector}
comes back carrying more attribute rows than it has geometries. Nothing
complains at the time; the next thing to touch the attributes fails instead,
a long way from the cause. Measured on one tile of a real study area, exactly
one of 6,871 differences was such a line, and it surfaced two function calls
later as \verb{[as,sf] coercion failed}.

Filtering in \code{sf} is what makes this safe: attributes are columns of the same
data frame as the geometry, so they cannot desynchronise.
}

\seealso{
\code{\link[=erase_polygons]{erase_polygons()}}, \code{\link[=overlay_left_join]{overlay_left_join()}}, \code{\link[=intersect_clean]{intersect_clean()}}
}
