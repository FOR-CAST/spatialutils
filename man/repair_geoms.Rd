% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repair_geoms.R
\name{repair_geoms}
\alias{repair_geoms}
\title{Repair invalid geometries efficiently}
\usage{
repair_geoms(v)
}
\arguments{
\item{v}{A \code{SpatVector}, or an object coercible to one via \code{\link[terra:vect]{terra::vect()}}.}
}
\value{
A \code{SpatVector} of valid, non-empty geometries, in the same CRS and
carrying the same attributes; repaired features keep their rows.
}
\description{
Validates a \code{SpatVector} once and passes \strong{only} the invalid geometries to
\code{\link[terra:makeValid]{terra::makeValid()}}, then recombines them with the already-valid majority.
This avoids running \code{makeValid()} over an entire layer -- slow on large
records (e.g. national fire perimeters) where only a small fraction of
geometries are invalid. Any geometry that remains invalid or empty after
repair is dropped, so the result contains only valid, non-empty geometries.
}
\examples{
bowtie <- terra::vect("POLYGON ((0 0, 2 2, 0 2, 2 0, 0 0))") # self-intersecting
terra::is.valid(bowtie)
terra::is.valid(repair_geoms(bowtie))
}
\seealso{
Other spatial helpers:
\code{\link[=calc_centroid]{calc_centroid()}},
\code{\link[=create_bbox]{create_bbox()}},
\code{\link[=extract_coords]{extract_coords()}},
\code{\link[=prep_agg_lonlat_raster]{prep_agg_lonlat_raster()}},
\code{\link[=save_gpkg]{save_gpkg()}},
\code{\link[=spatial_join_intersects]{spatial_join_intersects()}}
}
\concept{spatial helpers}
